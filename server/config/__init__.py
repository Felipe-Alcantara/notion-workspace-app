"""Projeto Django do servidor (settings, urls, wsgi/asgi).

Mantém só o cabeamento do framework. A configuração de valores vem de
``core.config`` (ambiente); a regra de negócio fica em ``services`` e a borda HTTP
em ``api``.
"""
